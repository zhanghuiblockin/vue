// initial state
const state = {
  all: ['etn', 'xmr', 'eth'],
  current: 'eth'
}

// getters
const getters = {}

// actions
const actions = {}

// mutations
const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}
